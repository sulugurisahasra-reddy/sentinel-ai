import { createFileRoute } from "@tanstack/react-router"; import { Portal } from "@/components/sentinel/portal";
export const Route=createFileRoute("/projects/$projectId")({head:({params})=>({meta:[{title:`${params.projectId} | MPLADS AI Sentinel`},{name:"description",content:"Explainable project risk, evidence and official verification workflow."},{property:"og:title",content:`${params.projectId} Project Risk Review`},{property:"og:description",content:"AI-assisted project analysis with practical verification actions."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Detail}); function Detail(){const {projectId}=Route.useParams();return <Portal view="projects" projectId={projectId}/>}